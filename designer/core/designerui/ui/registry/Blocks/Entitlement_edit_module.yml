Block:
  className: edit_module tablerow 
  children:
  - Block:
      className: field tablecell 
      body: {{jsreplace "ctx.data.Name"}}
  - Block:
      className: field tablecell 
      body: {{jsformat "this is %s" "ctx.data.UpdatedAt"}}
  - Block:
      className: field tablecell 
      children:
      - Action:
          name: editModule
          visibility: {{jsreplace "ctx.data.Local"}}
          params:
            moduleId: {{jsreplace "ctx.data.Module.Id"}}
          body: Edit Module
