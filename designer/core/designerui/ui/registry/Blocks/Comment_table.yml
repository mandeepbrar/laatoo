Block:
  className: comment_default
  children:
  - Block:
      className: field
      body: {{jsformat "some %s" "ctx.data.Post"}}
  - Block:
      className: field
      body: {{jsformat "some %s" "ctx.data.PostTitle"}}
