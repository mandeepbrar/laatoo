Block:
  className: user_default
  children:
  - Block:
      className: field
      body: {{jsreplace "ctx.data.Username"}}
  - Block:
      className: field
      body: {{jsformat "this is %s" "ctx.data.Id"}}
  - Block:
      className: field
      children:
      - Action:
          name: viewUser
          params:
            id: {{jsreplace "ctx.data.Id"}}
          body: View User
