Block:
  className: user_default
  children:
  - Block:
      className: field
      body: {{js "ctx.data.Username"}}
  - Block:
      className: field
      body: this is {{js "ctx.data.Id" "insert"}}
  - Block:
      className: field
      children:
      - Action:
          name: viewUser
          params:
            id: {{js "ctx.data.Id"}}
          body: View User
