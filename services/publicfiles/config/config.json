{
  "version":"0.0.1",
  "dependencies":{
    "staticfileserver":">=0.0.1",
    "filesystemstorage":">=0.0.1"
  },
  "objects":{
  },
  "params":{
    "files":"Config providing mapping of filename, internal path relative to storage directory, caching to be used for serving public files",
    "filestoragedir":"Directory where files to be served are stored",
    "instance":"Discriminator to distinguish multiple services created by multiple instances of the module"
  }
}
