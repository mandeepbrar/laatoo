name: update_form_{{default "instance" "object" | lower}}
authenticate: {{if exists "authenticate_update"}} {{var "authenticate_update"}} {{else}} true {{end}} 
config:
  skip: {{var "update_form_page" | not}}
route: {{if exists "update_form_route"}} {{var "update_form_route"}} {{else}} /update_form_{{default "instance" "object" | lower}}/:entityId {{end}}
component:
  type: form
  id: update_form_{{default "instance" "object" | lower}}
component:
  type: layout
  layout: 1col
  items:
  items:
    - type: html
      html: <h3>Update {{default "instance" "object" }}</h3>
    - type: form
      id: update_form_{{default "instance" "object" | lower}}
